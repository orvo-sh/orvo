// Code generated by `pnpm email:generate`. DO NOT EDIT.

type RenderBillingSubscriptionCompletedInput = {
	organizationName: string;
	planName: string;
};

function renderBillingSubscriptionCompletedEmail(input: RenderBillingSubscriptionCompletedInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>
      Thanks for subscribing to Orvo for ${input.organizationName}. You're all set.
    </p>
    <p>
      Your ${input.planName} plan is now active for the current billing period, so
      you can get back to shipping with logs, metrics, and traces flowing in one
      place.
    </p>
    <p>
      If you haven't already, this is a good time to connect your app and invite
      the rest of your team.
    </p>
    <p>Cheers,<br />The Orvo team</p>
    <p>
      P.S. If anything looks off in billing, just reply to this email and we'll
      help sort it out.
    </p>
  </body>
</html>
`;
}

type RenderBillingTrialExpiredInput = {
	organizationName: string;
};

function renderBillingTrialExpiredEmail(input: RenderBillingTrialExpiredInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>Your Orvo trial for ${input.organizationName} has ended.</p>
    <p>
      To keep your team monitoring apps without interruption, add a payment
      method from the billing page and we'll restore access right away.
    </p>
    <p>
      If you're still evaluating Orvo and need a hand deciding on a plan, reply
      to this email and we'll help you figure out the right fit.
    </p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderBillingTrialStartedInput = {
	organizationName: string;
	trialEnd: string;
};

function renderBillingTrialStartedEmail(input: RenderBillingTrialStartedInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>
      Your Orvo trial for ${input.organizationName} is now active, and you're ready
      to start exploring.
    </p>
    <p>
      Your trial runs until ${input.trialEnd}, so now is the perfect time to connect
      an app, send some telemetry, and see Orvo in action.
    </p>
    <p>
      If you have questions while getting set up, reply to this email and we'll
      help you get moving.
    </p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderBillingTrialWillEndInput = {
	organizationName: string;
	trialEnd: string;
};

function renderBillingTrialWillEndEmail(input: RenderBillingTrialWillEndInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>Your Orvo trial for ${input.organizationName} is ending soon.</p>
    <p>
      It wraps up on ${input.trialEnd}, so if you'd like to keep access going, head
      to billing and add a payment method before the deadline.
    </p>
    <p>
      If you're close but not fully set up yet, reply to this email and we'll
      help you get the most out of the rest of your trial.
    </p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderDestinationTestInput = {
	appName: string;
	destinationName: string;
};

function renderDestinationTestEmail(input: RenderDestinationTestInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>This is a test notification from Orvo for ${input.appName}.</p>
    <p>
      The destination <b>${input.destinationName}</b> is configured and able to
      receive email notifications.
    </p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderHeartbeatMissedInput = {
	appName: string;
	expectedEvery: string;
	grace: string;
	heartbeatName: string;
	lastCheckInAt: string;
};

function renderHeartbeatMissedEmail(input: RenderHeartbeatMissedInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>
      Orvo has not received the heartbeat <b>${input.heartbeatName}</b> for
      ${input.appName} on time.
    </p>
    <p>Expected every ${input.expectedEvery} with a grace window of ${input.grace}.</p>
    <p>Last check-in: ${input.lastCheckInAt}</p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderHeartbeatRecoveredInput = {
	appName: string;
	expectedEvery: string;
	heartbeatName: string;
	recoveredAt: string;
};

function renderHeartbeatRecoveredEmail(input: RenderHeartbeatRecoveredInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>The heartbeat <b>${input.heartbeatName}</b> for ${input.appName} has recovered.</p>
    <p>Expected every ${input.expectedEvery}.</p>
    <p>Recovered at: ${input.recoveredAt}</p>
    <p>Cheers,<br />The Orvo team</p>
  </body>
</html>
`;
}

type RenderOtpInput = {
	code: string;
	purpose: string;
};

function renderOtpEmail(input: RenderOtpInput): string {
	return `<!doctype html>
<html>
  <body>
    <p>Hey there,</p>
    <p>
      We got a request for your ${input.purpose} verification code. You'll need this:
    </p>

    <h2>${input.code}</h2>
    <p>If you didn't ask for this, just ignore the email. No worries.</p>
    <p>Cheers,<br />The Orvo team</p>
    <p>P.S. This code expires in 10 minutes, so be quick!</p>
  </body>
</html>
`;
}

type RenderThresholdAlertOpenedInput = {
	appName: string;
	entityName: string;
	observedValue: string;
	ruleName: string;
	signalType: string;
	threshold: string;
	window: string;
};

function renderThresholdAlertOpenedEmail(input: RenderThresholdAlertOpenedInput): string {
	return `<!DOCTYPE html>
<html>

<body>
  <p>Hey there,</p>
  <p>Orvo opened the alert <b>${input.ruleName}</b> for ${input.appName}.</p>
  <p>Signal: ${input.signalType}</p>
  <p>Entity: ${input.entityName}</p>
  <p>Observed value: ${input.observedValue}</p>
  <p>Threshold: ${input.threshold}</p>
  <p>Window: ${input.window}</p>
  <p>Cheers,<br />The Orvo team</p>
</body>

</html>
`;
}

type RenderThresholdAlertRenotifiedInput = {
	appName: string;
	entityName: string;
	observedValue: string;
	ruleName: string;
	signalType: string;
	threshold: string;
	window: string;
};

function renderThresholdAlertRenotifiedEmail(input: RenderThresholdAlertRenotifiedInput): string {
	return `<!DOCTYPE html>
<html>

<body>
  <p>Hey there,</p>
  <p>The alert <b>${input.ruleName}</b> for ${input.appName} is still firing.</p>
  <p>Signal: ${input.signalType}</p>
  <p>Entity: ${input.entityName}</p>
  <p>Observed value: ${input.observedValue}</p>
  <p>Threshold: ${input.threshold}</p>
  <p>Window: ${input.window}</p>
  <p>Cheers,<br />The Orvo team</p>
</body>

</html>
`;
}

type RenderThresholdAlertResolvedInput = {
	appName: string;
	entityName: string;
	observedValue: string;
	resolvedAt: string;
	ruleName: string;
	signalType: string;
};

function renderThresholdAlertResolvedEmail(input: RenderThresholdAlertResolvedInput): string {
	return `<!DOCTYPE html>
<html>

<body>
  <p>Hey there,</p>
  <p>The alert <b>${input.ruleName}</b> for ${input.appName} has resolved.</p>
  <p>Signal: ${input.signalType}</p>
  <p>Entity: ${input.entityName}</p>
  <p>Last observed value: ${input.observedValue}</p>
  <p>Resolved at: ${input.resolvedAt}</p>
  <p>Cheers,<br />The Orvo team</p>
</body>

</html>
`;
}

const templates = {
	"billing-subscription-completed": renderBillingSubscriptionCompletedEmail,
	"billing-trial-expired": renderBillingTrialExpiredEmail,
	"billing-trial-started": renderBillingTrialStartedEmail,
	"billing-trial-will-end": renderBillingTrialWillEndEmail,
	"destination-test": renderDestinationTestEmail,
	"heartbeat-missed": renderHeartbeatMissedEmail,
	"heartbeat-recovered": renderHeartbeatRecoveredEmail,
	"otp": renderOtpEmail,
	"threshold-alert-opened": renderThresholdAlertOpenedEmail,
	"threshold-alert-renotified": renderThresholdAlertRenotifiedEmail,
	"threshold-alert-resolved": renderThresholdAlertResolvedEmail,
} as const;

type TemplateMap = {
	"billing-subscription-completed": RenderBillingSubscriptionCompletedInput;
	"billing-trial-expired": RenderBillingTrialExpiredInput;
	"billing-trial-started": RenderBillingTrialStartedInput;
	"billing-trial-will-end": RenderBillingTrialWillEndInput;
	"destination-test": RenderDestinationTestInput;
	"heartbeat-missed": RenderHeartbeatMissedInput;
	"heartbeat-recovered": RenderHeartbeatRecoveredInput;
	"otp": RenderOtpInput;
	"threshold-alert-opened": RenderThresholdAlertOpenedInput;
	"threshold-alert-renotified": RenderThresholdAlertRenotifiedInput;
	"threshold-alert-resolved": RenderThresholdAlertResolvedInput;
};

type Template = {
	[K in keyof TemplateMap]: {
		template: K;
		props: TemplateMap[K];
	};
}[keyof TemplateMap];

const renderTemplate = (input: Template): string => {
	switch (input.template) {
		case "billing-subscription-completed":
			return renderBillingSubscriptionCompletedEmail(input.props);
		case "billing-trial-expired":
			return renderBillingTrialExpiredEmail(input.props);
		case "billing-trial-started":
			return renderBillingTrialStartedEmail(input.props);
		case "billing-trial-will-end":
			return renderBillingTrialWillEndEmail(input.props);
		case "destination-test":
			return renderDestinationTestEmail(input.props);
		case "heartbeat-missed":
			return renderHeartbeatMissedEmail(input.props);
		case "heartbeat-recovered":
			return renderHeartbeatRecoveredEmail(input.props);
		case "otp":
			return renderOtpEmail(input.props);
		case "threshold-alert-opened":
			return renderThresholdAlertOpenedEmail(input.props);
		case "threshold-alert-renotified":
			return renderThresholdAlertRenotifiedEmail(input.props);
		case "threshold-alert-resolved":
			return renderThresholdAlertResolvedEmail(input.props);
	}
};

export {
	renderBillingSubscriptionCompletedEmail,
	renderBillingTrialExpiredEmail,
	renderBillingTrialStartedEmail,
	renderBillingTrialWillEndEmail,
	renderDestinationTestEmail,
	renderHeartbeatMissedEmail,
	renderHeartbeatRecoveredEmail,
	renderOtpEmail,
	renderThresholdAlertOpenedEmail,
	renderThresholdAlertRenotifiedEmail,
	renderThresholdAlertResolvedEmail,
	renderTemplate,
	templates,
};

export type {
	RenderBillingSubscriptionCompletedInput,
	RenderBillingTrialExpiredInput,
	RenderBillingTrialStartedInput,
	RenderBillingTrialWillEndInput,
	RenderDestinationTestInput,
	RenderHeartbeatMissedInput,
	RenderHeartbeatRecoveredInput,
	RenderOtpInput,
	RenderThresholdAlertOpenedInput,
	RenderThresholdAlertRenotifiedInput,
	RenderThresholdAlertResolvedInput,
	Template,
};
